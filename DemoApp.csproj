<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- This package does NOT exist on nuget.org — intentional failure -->
    <PackageReference Include="FakePackage.NotExist" Version="9.9.9" />
  </ItemGroup>

</Project>